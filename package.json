{
  "name": "jspixelize",
  "private": true,
  "scripts": {
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "pretest": "bun typecheck && bun lint",
    "lint": "oxlint",
    "format": "dprint fmt",
    "start": "bun server.ts",
    "build": "bun build.ts"
  },
  "devDependencies": {
    "@types/bun": "1.3.0",
    "dprint": "0.50.2",
    "oxlint": "1.24.0"
  },
  "peerDependencies": {
    "typescript": "5.9.3"
  }
}
